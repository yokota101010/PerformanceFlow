# Implementation Plan: Domain Model & CRUD Matrix Alignment

**Branch**: `012-domain-crud-alignment` | **Date**: 2026-07-28 | **Spec**: [spec.md](file:///mnt/c/ai-code/GitHub-Spec-Kit-first-step/PerformanceFlow/specs/012-domain-crud-alignment/spec.md)

**Input**: Feature specification from `/specs/012-domain-crud-alignment/spec.md`

**Note**: This template is filled in by the `/speckit-plan` command. See `.specify/templates/plan-template.md` for the execution workflow.

## Summary

`domain-model.md`（15のドメイン集約正本）および `crud-matrix.md`（集約×機能のマトリクス）の変更に適合するよう、アプリケーションのドメイン層、ユースケース層、UIコンポーネントを改修・最適化します。「単一トランザクション・単一集約原則」を厳格適用し、サマリ集約のプル型オンデマンド算出を確立します。

## Technical Context

<!--
  ACTION REQUIRED: Replace the content in this section with the technical details
  for the project. The structure here is presented in advisory capacity to guide
  the iteration process.
-->

**Language/Version**: TypeScript 5.x

**Primary Dependencies**: React 18+, Vite, Lucide-react

**Storage**: Browser LocalStorage (ブラウザ完結型・ローカル永続化)

**Testing**: Vitest / React Testing Library

**Target Platform**: Standalone Web App (Browser / SPA, `file://` スキーム起動サポート)

**Project Type**: Web Application (Single SPA)

**Performance Goals**: クエリ/オンデマンド算出画面のロード時間 < 1秒

**Constraints**: 単一トランザクション・単一集約の遵守、プル型非同期結果整合性、ブラウザ完結型

**Scale/Scope**: 15ドメイン集約（`domain-model.md` 準拠）、F01〜F14全機能の集約境界完全適合

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- **1. ドメイン層・アプリケーション層・インフラ層の3層依存ルール**: PASS
- **2. 単一トランザクション・単一集約の原則**: PASS (各 Command ユースケースは単一集約のみ変更)
- **3. 結果整合性の許容とプル型同期（オンデマンド再計算）**: PASS (Query ユースケースで動的集計)
- **4. ブラウザ完結型（スタンドアロンSPA）**: PASS
- **5. エンティティのクラス化と強制的制約検証**: PASS (ドメインコンストラクタで検証)

*Gate Result*: ALL PASSED

## Project Structure

### Documentation (this feature)

```text
specs/012-domain-crud-alignment/
├── plan.md              # This file
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output
├── quickstart.md        # Phase 1 output
├── contracts/           # Phase 1 output
│   └── usecases.md
└── tasks.md             # Phase 2 output (to be generated by /speckit-tasks)
```

### Source Code (repository root)
<!--
  ACTION REQUIRED: Replace the placeholder tree below with the concrete layout
  for this feature. Delete unused options and expand the chosen structure with
  real paths (e.g., apps/admin, packages/something). The delivered plan must
  not include Option labels.
-->

```text
src/
├── domain/               # Pure domain entities, value objects, and repository contracts
│   ├── models/           # 15 Domain Aggregates & Entities
│   └── repositories/     # Domain Repository Interfaces
├── application/          # UseCases and Query Services
│   ├── usecases/         # Command UseCases (Single Aggregate mutation)
│   └── queries/          # Query Services (On-demand summary calculation)
├── infrastructure/       # Browser LocalStorage repositories
│   └── storage/
└── ui/                   # React components & pages (F01 - F14)
    ├── components/
    └── pages/
```

**Structure Decision**: Standard SPA structure with strict 3-tier DDD separation (domain, application, infrastructure, ui).

## Complexity Tracking

> Violationなしのため項目記入不要
